# Michonne AI Chatbot

Backend and chatbot interface for the memory-room project.

## Setup
```bash
npm install
npm run dev    # Development
npm run build  # Production
npm start      # Run server
```

## Deploy to Render
- Build: `npm install && npm run build`
- Start: `npm start`
